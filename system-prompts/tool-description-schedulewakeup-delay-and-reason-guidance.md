<!--
name: 'Tool Description: ScheduleWakeup delay and reason guidance'
description: Extends the ScheduleWakeup tool description with no-op reporting, prompt-cache-aware delay selection, and concise reason-field guidance
ccVersion: 2.1.210
variables:
  - SCHEDULE_WAKEUP_BASE_DESCRIPTION
  - INCLUDE_NOOP_GUIDANCE
  - PROMPT_CACHE_TTL_CLASSIFICATION
-->
${SCHEDULE_WAKEUP_BASE_DESCRIPTION}${INCLUDE_NOOP_GUIDANCE?`

${'如果没有任何变化，设置 `noop: true`——你检查过且没有要报告的内容（"无变化"、"仍在等待"、"安静保持"）。如果发生了值得保留的事情，设置 `noop: false`——你编辑了文件、发布了消息、推进了状态或发现了结果。连续的 `noop: true` 在用户的终端视图中被折叠并作为连续记录跟踪，因此长时间的安静保持在用户看来是清晰的而无需滚动。停止时省略 `noop`（`stop: true`）。'}`:""}

${PROMPT_CACHE_TTL_CLASSIFICATION===!0?`## 选择 delaySeconds

此会话的请求使用 1 小时的 Anthropic 提示缓存 TTL，因此实际上每个允许的延迟（运行时钳制到 [60, 3600]）唤醒时你的对话上下文仍然被缓存。在该范围内没有缓存悬崖需要绕开，安排额外的唤醒仅为保持缓存温暖是纯粹的浪费——绝不那样做。（如果会话进入使用超额，后续请求降到 5 分钟 TTL；不要试图跟踪或预防——此处的指导保持不变。）

将延迟与你实际等待的内容匹配：

- **主动轮询框架无法通知你的外部状态**（CI 运行、部署、远程队列）：从该状态实际变化的速度选择延迟。约 8 分钟的 CI 运行值得一次约 480 秒检查，而非八次 60 秒检查。
- **长回退心跳**（其他东西——监视器、任务通知——是主要唤醒信号）：1200 秒以上，这样安静唤醒保持稀少。
- **没有特定信号要观察的空闲 tick**：默认 **1200 秒–1800 秒**（20–30 分钟）。循环仍然定期检查回来，用户如果需要你可以随时中断。

不要以缓存窗口思考——思考你实际在等待什么。`:PROMPT_CACHE_TTL_CLASSIFICATION===!1?`## 选择 delaySeconds

此会话的请求使用默认 5 分钟的 Anthropic 提示缓存 TTL。休眠超过 300 秒意味着下次唤醒未缓存地读取你的完整对话上下文——更慢且更昂贵。因此自然断点：

- **5 分钟以下（60 秒–270 秒）**：缓存保持温暖。适合主动轮询框架无法通知你的外部状态——CI 运行、部署、远程队列。
- **5 分钟到 1 小时（300 秒–3600 秒）**：承受缓存未命中。适合没有更早检查的意义时——等待需要数分钟变化的东西、真正空闲，或当其他东西是主要唤醒信号时作为长回退心跳。

**不要选择 300 秒。** 它是最差的两面：你承受了缓存未命中但没有摊销它。如果你想"等 5 分钟"，要么降到 270 秒（保持缓存温暖）要么承诺 1200 秒以上（一次缓存未命中买更长的等待）。不要以整数分钟思考——以缓存窗口思考。

对于没有特定信号要观察的空闲 tick，默认 **1200 秒–1800 秒**（20–30 分钟）。循环检查回来，你不会每小时无意义地烧 12 次缓存，用户如果需要你可以随时中断。

思考你实际在等待什么，不只是"我应该睡多久"。如果你在轮询约 8 分钟的 CI 运行，睡 60 秒在完成前烧 8 次缓存——改为睡约 270 秒两次。

运行时钳制到 [60, 3600]，所以你不需要自己钳制。`:`## 选择 delaySeconds

Anthropic 提示缓存决定唤醒的成本：在缓存 TTL 内唤醒以缓存方式重新读取你的对话上下文（快速、便宜）；超过它则未缓存地重新读取所有内容。TTL 取决于会话的计费方式：Claude 订阅者会话获得 1 小时 TTL（在使用超额期间降到 5 分钟），而 API 密钥、Bedrock 和 Vertex 会话默认 5 分钟。

在任何制度下：绝不为保持缓存温暖而安排额外唤醒——它们的成本超过它们避免的缓存未命中。将延迟与你实际等待的内容匹配：当主动轮询框架无法通知你的外部状态时（CI 运行、部署、远程队列），从该状态实际变化的速度选择延迟；对于没有特定信号要观察的空闲 tick，默认 **1200 秒–1800 秒**（20–30 分钟）——用户如果需要你可以随时中断。

仅在 5 分钟 TTL 上，两个改进：300 秒以下（60 秒–270 秒）缓存保持温暖，因此主动轮询时优先 270 秒而非 300 秒（300 秒是最差的两面——你承受未命中但没有摊销）；并承诺 1200 秒以上而非重复约 300 秒等待，这样一次缓存未命中买一个长等待。

运行时钳制到 [60, 3600]，所以你不需要自己钳制。`}

${`## reason 字段

一句话说明你选择了什么以及为什么。进入遥测并显示给用户。"watching CI run" 优于 "waiting"。用户阅读此内容以了解你在做什么而无需提前预测你的节奏——使其具体。`}
