<!--
name: '工具描述：报告代码审查发现'
description: 以类型化列表形式报告已验证的代码审查发现，供宿主 UI 渲染的工具描述
ccVersion: 2.1.196
-->
以类型化列表形式报告代码审查发现，以便宿主 UI 渲染它们。仅当激活的代码审查指示你使用此工具报告发现时才使用；否则遵循那些指示指定的任何输出格式。报告审查结果时，调用一次，将已验证的发现按严重程度从高到低排列（如果没有发现通过验证则为空数组），并且不要同时以文本形式打印发现。在应用修复后重新报告时（仅当应用指示要求时），将每个发现的 `outcome` 设置为实际发生的情况。
