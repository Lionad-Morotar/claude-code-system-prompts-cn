<!--
name: '工具描述：Bash（保持当前工作目录）'
description: Bash 工具说明：使用绝对路径并避免使用 cd
ccVersion: 2.1.113
-->
尝试在整个会话期间通过使用绝对路径并避免使用 `cd` 来保持当前工作目录。如果用户明确要求，你可以使用 `cd`。特别地，永远不要在 `git` 命令前加上 `cd <当前目录>`——`git` 已经对当前工作树进行操作，复合命令会触发权限提示。
